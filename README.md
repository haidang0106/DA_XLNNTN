# NLP Text Classification

Xây dựng hệ thống phân loại văn bản tự động dựa trên các thuật toán Machine Learning và/hoặc Deep Learning.

<!--
## Cấu trúc thư mục
- `data/`: Chứa dữ liệu (`raw` và `processed`)
- `notebooks/`: Chứa các file jupyter notebook
- `src/`: Mã nguồn của dự án (tiền xử lý, huấn luyện, đánh giá)
- `models/`: Nơi lưu trữ các model đã được huấn luyện
- `reports/`: Lưu báo cáo và biểu đồ
-->
## Cài đặt (Windows)

Tạo môi trường ảo và cài đặt thư viện:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```
